const DocumentRepositoryInterface = () => {
  const create = (document) => {};
  const findById = (id) => {};
  const find = (id) => {};
  const findByOwnerId = (owner_id) => {};
  return { create, findById, find, findByOwnerId };
};
